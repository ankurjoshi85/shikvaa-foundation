## Packages
framer-motion | Page transitions and scroll-triggered animations for a premium feel

## Notes
- `api.contact.create` endpoint is expected to handle form submissions (contact, workshop, volunteer, partner).
- i18n is implemented via a simple React Context storing language preference in localStorage.
- The theme toggle requires `next-themes` which is already installed.
- Images from `@assets` are used for branding and hero sections.
